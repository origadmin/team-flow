# team-flow 会话交接文档

> 生成时间: 2026-05-09 | 会话任务: team-flow-6x9.26

## 当前项目状态

- **项目**: team-flow (v2.3 skill optimization)
- **仓库**: `D:\workspace\project\golang\origadmin\framework`
- **子仓库**: `D:\workspace\project\golang\origadmin\framework\projects\team-flow`
- **版本**: v2 (beads-native)
- **Epic**: team-flow-6x9 (v2.3 skill optimization)

## 本会话已完成的工作

### 1. dev.md v1/v2 双格式修复
- **问题**: dev.md 只有 v1 格式 (F001)，导致 AI 在 v2 项目中也使用 F001 格式
- **修复**: dev.md TaskPool 格式改为版本路由，v1 用 `F001`/`B001`，v2 用 `beads-id#cr-index`
- **提交**: framework 仓库 `7eb5e445`

### 2. SKILL.md Progressive Disclosure 优化
- **问题**: SKILL.md 449 行/~5K tokens，接近 500 行上限
- **修复**: 提取 3 个 reference 文件，SKILL.md 精简到 193 行/~2K tokens (-57%)
  - `references/commands.md` — 完整命令参考 + 路由表
  - `references/path-resolution.md` — 路径变量和锚点规则
  - `references/agent-mapping.md` — Agent 映射 + 角色配置 + MILESTONES
- **提交**: team-flow 子仓库 + framework 仓库

### 3. Toolchain Guard 添加
- **问题**: AI 使用 npm 而非 bun，违反 project.md 定义的 toolchain
- **根因**: Toolchain 规则只在 triage.md 分发模板中，SKILL.md 和 dev.md 都没有
- **修复**: 在三个层级加入 Toolchain Guard:
  - SKILL.md HARD CONSTRAINTS
  - dev.md Toolchain Guard (含 bun/npm/pnpm 对照表)
  - triage.md 分发模板 (已有，保持不变)
- **提交**: framework 仓库 `487d2ef6`

### 4. flow task append 命令格式修复
- **问题**: 文档写 `flow task append {id} --speaker {role} --content "..."` 但 bd note 不支持这些参数
- **修复**: 改为 `flow task append {id} "[{role}] message text"`，直接映射到 `bd note <id> [text...]`
- **提交**: team-flow 子仓库 `b010e28` + framework 仓库 `94d1ddc3`

### 5. docs 目录重组
- v1 docs: `docs/` → `docs/v1/`
- v2 docs: `team/v2/docs/` → `docs/v2/`
- 安装副本同步: `.trae/skills/team-flow/docs/` → `docs/v2/`

## 识别但未修复的问题

### 流程违规问题 (高优先级)
1. **Triage 直接执行而非分发**: 文档/SKILL 修改属于 Triage 职责范围不需要分发 Dev，但代码实现任务应该分发
2. **cr-index 未累加**: 之前会话中 Status Line 一直用 `team-flow-6x9` 没有 #cr-index，本会话末尾才修正
3. **Dev 角色直接和用户对话**: Dev 是子 Agent 角色，不应和用户讨论，所有交互应通过 Triage

### 待完成的 beads 任务
| ID | 优先级 | 标题 | 状态 |
|----|--------|------|------|
| team-flow-6x9.16 | P2 | Evaluate CLI vs MCP mode for flow tools | open |
| team-flow-6x9.24 | P3 | Discussion: extend cr-index pattern to Bug R-iteration | open |
| team-flow-6x9.26 | P1 | Fix: SKILL.md/dev.md rule gaps causing AI violations | open (本会话创建) |
| team-flow-7bd | P0 | Add mandatory test verification steps to bugfix workflow | open |
| team-flow-awa | P1 | Fix bd CLI PATH registration in init.go | open |
| team-flow-zab | P1 | Add UI verification template with screenshot rules | open |

### Token 优化 (未执行)
- 分析完成但实际优化未执行:
  - 提取 dispatch templates (-5K tokens)
  - 拆分 shared.md (-3K tokens)
  - 去重 commands (-2K tokens)
  - 总潜在优化: ~43%

## 关键文件路径

| 文件 | 路径 | 说明 |
|------|------|------|
| SKILL.md (源) | `projects/team-flow/team/v2/SKILL.md` | v2 skill 入口，193 行 |
| SKILL.md (安装) | `.trae/skills/team-flow/SKILL.md` | 安装副本，需与源同步 |
| dev.md | `.trae/rules/dev.md` | workspace rules，每次会话加载 |
| team-flow.md | `.trae/rules/team-flow.md` | skill 触发规则 |
| project.md | `.team/project.md` | 项目配置 (含 TOOLCHAIN 定义) |
| references/ | `projects/team-flow/team/v2/references/` | 3 个参考文件 |
| task.go | `projects/team-flow/internal/task/task.go` | flow task CLI 实现 |
| main.go | `projects/team-flow/cmd/flow/main.go` | CLI 入口 |

## 两个仓库的提交关系

- **framework 仓库** (父): 包含 `.trae/skills/team-flow/` (安装副本) + `.trae/rules/`
- **team-flow 子仓库** (子): 包含 `projects/team-flow/team/v2/` (源文件)
- 修改源文件后，必须手动同步安装副本
- 两个仓库独立提交

## 继续工作提示词

```
你是 team-flow 项目的 Triage 角色。项目是 v2 beads-native 模式。

当前任务: team-flow-6x9.26 — Fix: SKILL.md/dev.md rule gaps causing AI violations

已完成: 4 个修复已提交 (dev.md v1/v2双格式 / SKILL.md Progressive Disclosure / Toolchain Guard / append命令格式)

待处理:
1. 关闭 team-flow-6x9.26 (修复已完成，需要 flow task close)
2. 处理 team-flow-6x9.16 (P2: Evaluate CLI vs MCP mode)
3. 处理 team-flow-7bd (P0: Add mandatory test verification steps to bugfix workflow)
4. Token 优化执行 (triage.md dispatch template 提取 / shared.md 拆分 / commands 去重)

关键规则:
- 你是 Triage，所有用户交互由你负责
- 代码实现任务分发 Dev 子 Agent，文档/SKILL 修改由 Triage 直接执行
- Status Line 格式: [Role: Triage | TaskPool: {beads-id}#{cr-index} | Phase: {phase} | Asset: team-flow]
- 每次对话用 flow task append 记录: flow task append {id} "[Triage] message"
- Toolchain: 读 .team/project.md，当前项目 frontend 用 bun，禁止 npm
- 源文件在 projects/team-flow/team/v2/，修改后必须同步 .trae/skills/team-flow/ 安装副本
- 两个仓库独立提交: team-flow 子仓库 + framework 父仓库

读取交接文档: projects/team-flow/docs/v2/handoff-2026-05-09.md
```
