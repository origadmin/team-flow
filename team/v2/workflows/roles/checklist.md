# 各阶段检查清单与测试报告模板

> **适用**: DevTestOps 各阶段的质量检查和测试报告输出
> **关联**: devtestops.md（各阶段引用）、test-levels.md（准入基线）

---

## 1. 覆盖场景清单（每次测试必须逐项检查）

- [ ] 正常流程（happy path）
- [ ] 异常/错误流程（每种错误类型独立验证）
- [ ] 降级/fallback 路径
- [ ] 边界输入（空值/零值/超长/非法/特殊字符/前后空格/大小写）
- [ ] 并发/重复提交（Loading 锁/幂等）
- [ ] 状态流转（合法状态 + 非法/未定义状态）
- [ ] 数据一致性（PRD↔代码 / Store↔UI / 缓存↔DB）
- [ ] 幂等/重复请求
- [ ] 超时/重试/部分失败
- [ ] 历史数据兼容/回归
- [ ] 安全（XSS/注入/权限绕过/会话管理/敏感信息泄露）

不适用的场景标注"不适用"并附原因，不能留空。

---

## 2. 测试用例文档模板

生成测试用例时，按以下结构输出到 `tests/testcases/{module}-testcases.md`：

```markdown
# {模块名} 测试用例

> 生成时间：{timestamp}
> PRD 来源：{prd_file_paths}
> 源码范围：{scanned_files}
> 测试分层方法论：基于 DevTestOps 提测准入基线

---

## 变更类型→测试级别映射

（判定当前变更类型，列出必须覆盖的测试级别）

# 第一章：单元测试（P0）
（UT-001, UT-002...）

# 第二章：组件测试（P0）
（CT-001, CT-002...）

# 第三章：模块测试（P1）
（MT-001, MT-002...）

# 第四章：集成测试（P1）
（IT-001, IT-002...）

# 第五章：API 接口测试 / Mock 契约验证（P1）
（API-001, API-002...）

# 第六章：E2E 测试（P2）
（E2E-001, E2E-002...）

# 第七章：系统测试（P2）
（SYS-001, SYS-002...）

# 第八章：安全测试（P1）
（SEC-001, SEC-002...）

# 第九章：兼容性与回归测试（P2）
（REG-001, REG-002...）

# 第十章：一致性问题与自动化验证
（GAP 表格 + 验证方式 + 一键验证命令）

# 统计总览
（按层级/按场景/覆盖率）
```

### 每条用例必须包含

- **编号**：{层级缩写}-{三位数字}（如 UT-001, CT-010, MT-001）
- **用例标题**：一句话描述
- **前置条件**（如有）
- **操作步骤**
- **预期结果**：必须具体可验证，不写"正常显示""符合预期"
- **数据来源**：PRD §章节 / 源码 file:line
- **优先级**：P0（核心流程）/ P1（重要分支）/ P2（边界和异常）

---

## 3. PRD↔代码交叉比对规则

生成用例时必须做以下交叉比对，发现问题标记到一致性问题章节：

| 比对方向 | 发现 | 标记 |
|---------|------|------|
| PRD 定义了，代码已实现 | 正常 | 生成对应测试用例 |
| PRD 定义了，代码未实现 | 缺失 | `[PRD 未覆盖]` |
| 代码实现了，PRD 未定义 | 偏离 | `[代码超出 PRD]` |
| PRD 与代码矛盾 | 不一致 | `[不一致]` — 按两种预期各出一条用例 |
| Mock 数据与 PRD 不一致 | 契约违规 | `[Mock 缺失]` 或 `[Mock 不一致]` |

---

## 4. 测试报告模板

执行测试后，生成报告到 `tests/testcases/{module}-test-report.md`：

```markdown
# {模块名} 测试报告

> 执行时间：{timestamp}
> 执行环境：{os} / {runtime} / {test_framework} {version}

---

## 总体健康度

（用红绿灯标识各维度状态）

  核心流程        🟢/🟡/🔴
  状态化反馈      🟢/🟡/🔴
  ...

**整体判定：🟢 绿灯 / 🟡 黄灯 / 🔴 红灯**

## 执行概览

| 指标 | 数值 |
|------|------|
| 测试用例 | X 个 |
| 通过 | 🟢 X |
| 失败 | 🔴 X |
| 跳过 | ⚪ X |

## 按测试层级执行结果

（每条用例标注 🟢/🟡/🔴）

## 一致性问题验证结果

（每个 GAP 标注验证方式和确认结果）

## 覆盖率仪表盘

（ASCII 进度条 + 红绿灯）

## 结论

### 🟢 通过项
### 🔴 必须修复
### 🟡 建议修复
### 🟡 待产品决策
```

---

## 5. 红绿灯判定规则

| 状态 | 条件 |
|------|------|
| 🟢 绿灯 | 测试通过，行为符合 PRD 预期 |
| 🟡 黄灯 | 测试通过但发现非阻塞问题（建议修复 / 待产品决策 / 部分覆盖） |
| 🔴 红灯 | 测试失败 / PRD 与代码不一致且影响功能 / 安全风险 |
| ⚪ 未执行 | 需要特定环境或工具，当前未执行 |

### 整体判定规则

- 全部 🟢 → **🟢 绿灯**：可提测/可交付
- 有 🟡 无 🔴 → **🟡 黄灯**：核心功能正常，有非阻塞问题需跟进
- 有 🔴 → **🔴 红灯**：有阻塞问题，不建议提测/交付
