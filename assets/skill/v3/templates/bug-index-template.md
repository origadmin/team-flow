# B{xxx}: {Bug 标题} — 迭代索引

> ⚠️ **AI 读取规则**：必须先读本文件，只读 `current_iteration` 指向的 R 目录。禁止读取历史 R 目录。

## beads 关联

| 字段 | 值 |
|------|-----|
| Issue ID | `<beads-id>` |
| task ID | B{xxx} |
| Current R | R{n} |

## 迭代历史

| R | 状态 | 根因类型 | 修复摘要 | 验证结果 | 日期 |
|---|------|---------|---------|---------|------|
| R1 | 🔴 失败 | {根因类型} | {修复摘要} | ❌ {失败原因} | {date} |
| R2 | 🔴 失败 | {根因类型} | {修复摘要} | ❌ {失败原因} | {date} |
| R3 | 🟢 当前 | {根因类型} | {修复摘要} | ⏳ 待验证 | {date} |

## 当前迭代

**当前 R**: R{n}
**目录**: `R{n}/`
**状态**: {phase:analyze / phase:implement / phase:verify / phase:review}

AI 只需读取以下文件：
- `R{n}/RCA.md` — 根因分析
- `R{n}/TEST_CASE.md` — 复现测试用例
- `R{n}/SCOPE.md` — 变更报告

## 历史 R（禁止读取）

以下 R 迭代已失败/过期，**AI 禁止读取**（仅作人类参考）：

| R | 失败原因 | 失败日期 |
|---|---------|---------|
| R1 | {原因} | {date} |
| R2 | {原因} | {date} |

---

## 索引维护规则

1. **新增 R 迭代时**：在"迭代历史"表新增一行，更新 `current_iteration`
2. **R 验证失败时**：标记状态为 🔴 失败，填写失败原因，新增下一行 R
3. **R 验证通过时**：标记状态为 🟢 通过，Bug 可关闭
4. **AI 读取规则**：只读 current_iteration 指向的 R 目录，禁止读历史 R
